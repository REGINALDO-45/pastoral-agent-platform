# Auditoria do GitHub Actions run 32395892553

Fonte primária: [GitHub Actions run 32395892553](https://github.com/REGINALDO-45/pastoral-agent-platform/actions/runs/32395892553).

O run foi executado no SHA `f27fd161487846d8f4d993c8c8b84798c1156086`, no workflow histórico Hermes-OFF, com o job `Isolated MySQL 8.4 sandbox`. O job iniciou MySQL 8.4.11, aplicou migrations com sucesso, validou boundary local, manteve Hermes e THÁNOS desligados e executou a suíte completa.

Resultado: `50 passed` arquivos e `211 passed` testes. O typecheck e o build também passaram. O run terminou `success`.

A correção final inclui expectativa do provider efetivo `legacy` para tenant não allowlisted, snapshot/restore determinístico de memberships e restauração explícita da role para `pastor` antes das assertions de negação. Nenhuma mudança foi feita em `enforceHermesEligibility`, no workflow mock ou em paths de produção.

A auditoria local de configuração registrou `HERMES_BASE_URL=absent` e `HERMES_API_KEY=absent`. A API do GitHub não listou o Environment `hermes-sandbox` no repositório auditado; o workflow live permanece não executado. O runner, sem credenciais, terminou com `{"ok":false,"failure":"hermes_base_url_missing"}`. O runner agora valida a falha controlada contra `config.hermes.retries + 1`, que corresponde a duas tentativas quando o workflow live estiver configurado com `HERMES_RETRIES=1`.
