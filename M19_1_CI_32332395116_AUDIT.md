# Auditoria do GitHub Actions run 32332395116

Fonte primária: [GitHub Actions run 32332395116](https://github.com/REGINALDO-45/pastoral-agent-platform/actions/runs/32332395116).

O run foi executado no SHA `341dd82aae229eef6d74ef075a03aa1fe40f463c`. O job `Isolated MySQL 8.4 sandbox` iniciou MySQL `8.4.11`, registrou `/usr/sbin/mysqld: ready for connections`, criou o banco efêmero `thanos_hermes_ci`, passou o boundary check local e concluiu as migrations.

O `pnpm test` executou 50 arquivos: 49 passaram e somente `server/pastoral/router.integration.test.ts` falhou, com 3 testes falhos e 208 testes aprovados de 211. A primeira divergência registrada foi no teste de configuração do Gateway: a expectativa antiga exigia provider `hermes`, enquanto o provider recebido foi `legacy`. Isso é compatível com `enforceHermesEligibility`, pois o tenant B não está na allowlist Hermes.

As outras duas falhas foram assertions de autorização no mesmo arquivo. O teste `protege o status de integrações e audita o teste Hermes sem revelar segredos` esperava rejeição para `callerB`, mas recebeu resposta; o teste `persiste a habilitação somente para a organização do administrador e bloqueia mudanças por papéis não administrativos` esperava rejeição para o catálogo, mas recebeu resposta. A sequência de mutações de membership no arquivo alterava o tenant B para `admin` e podia não restaurar a role se uma assertion anterior falhasse.

Conclusão histórica: o MySQL estava saudável; o run não falhou por indisponibilidade do banco. A correção deve reconciliar a expectativa persistida `provider=hermes` com o provider efetivo governado `legacy` e garantir cleanup de memberships com `try/finally` ou equivalente. Não alterar `enforceHermesEligibility` nem adicionar o tenant B à allowlist apenas para satisfazer os testes.
