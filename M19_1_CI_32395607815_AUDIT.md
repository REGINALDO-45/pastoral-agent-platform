# Auditoria do GitHub Actions run 32395607815

Fonte primária: [GitHub Actions run 32395607815](https://github.com/REGINALDO-45/pastoral-agent-platform/actions/runs/32395607815).

O run foi executado no SHA `ff83ab066721f61eeaed51b601e62ee50d6093bd`, com o job `Isolated MySQL 8.4 sandbox`. MySQL 8.4 e o boundary CI foram executados; o banco não foi reportado como indisponível.

Resultado do `pnpm test`: 50 arquivos, sendo 49 aprovados e 1 com falha; 210 testes aprovados de 211. A única falha restante está em `server/pastoral/router.integration.test.ts`, no teste `persiste configuração do Gateway pelo admin, a resolve por tenant e nunca devolve segredos`, na assertion final que esperava `callerB.pastoral.updateAgentSettings(...)` rejeitar.

A resposta recebida foi `{ status: 'online', provider: 'legacy', ... }`. A causa é que o teste, após reconciliar o provider efetivo Hermes de tenant B para `legacy`, também havia promovido tenant B a `admin` dentro do mesmo cenário antes da assertion final. O cleanup com `finally` restaura o estado ao fim do teste, mas não pode tornar a própria assertion final negativa enquanto B ainda está admin.

Correção pendente: restaurar explicitamente a role do tenant B para `pastor` antes da assertion final de `updateAgentSettings`, preservando o snapshot/restore e sem alterar `enforceHermesEligibility` ou a política de autorização.
