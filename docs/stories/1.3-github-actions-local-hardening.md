# Story 1.3 — Hardening local do GitHub Actions

## Status

Patch local de hardening concluído. Nenhuma configuração remota foi alterada.

## Controle

- Control ID: `NXA-NATURAL-BF77F23584C8`
- Executor: NEXUS autonomous development worker (local)

## Objetivo

Fixar actions de repositório em commits imutáveis, sem atualizar versões funcionais, manter a versão legível em comentário e exigir digest `sha256` imutável para actions Docker. A regressão local deve analisar toda referência `uses:` e impedir refs móveis, inclusive `docker://` por tag ou sem digest.

## Acceptance Criteria

- [x] Workspace, Git e todos os arquivos em `.github/workflows` inspecionados antes das alterações.
- [x] Cada ref atualmente usada foi comprovada no repositório oficial da action.
- [x] Actions externas fixadas em SHAs completos de 40 caracteres, com versão legível em comentário.
- [x] Actions Docker exigem digest `sha256`; referências por tag ou sem digest são rejeitadas.
- [x] Nenhuma action ou versão funcional nova foi introduzida.
- [x] Branch protection não foi reconfirmada: `gh auth status` informou token inválido e nenhuma autenticação foi tentada.
- [x] Regressão focalizada, typecheck, build, parse YAML e `git diff --check` executados.
- [x] Diff revisado e status Git final registrado.

## Tasks

- [x] Auditar os workflows e inventariar todas as referências `uses:`.
- [x] Resolver refs móveis nos repositórios oficiais e tratar corretamente tags anotados.
- [x] Fixar os SHAs e preservar versões legíveis.
- [x] Adicionar regressão contra refs externas móveis, incluindo validação de digest para Docker actions.
- [x] Executar gates locais e self-review.

## Arquivos da Story 1.3

- `.github/workflows/thanos-hermes-sandbox.yml`
- `server/githubActionsHardening.test.ts`
- `docs/stories/1.3-github-actions-local-hardening.md`

## Artefatos preexistentes fora do escopo

Os arquivos abaixo pertencem à Story 1.2 e ao respectivo runbook. Já estavam no workspace, não foram editados, movidos ou apagados e não integram o escopo nem a File List da Story 1.3:

- `docs/stories/1.2-hermes-m19-oracle-sandbox.md`
- `docs/runbooks/hermes-m19-oracle-sandbox.md`

## Limites

Somente um checkpoint Git local faz parte desta story. Push, merge, deploy, workflow dispatch, migration, mudança de branch protection ou qualquer outra mutação remota permanecem fora do escopo.

## Evidência

- Inventário completo: um workflow e três referências externas `uses:`.
- Refs oficiais resolvidos por `git ls-remote --tags` nos repositórios das actions; o tag anotado do pnpm foi fixado no commit peeled, não no objeto-tag.
- Branch protection não foi reconfirmada nesta tentativa: `gh auth status` reportou que o token do usuário ativo é inválido. Nenhum login ou outra tentativa de autenticação foi executado.
- `node_modules/.bin/vitest run server/githubActionsHardening.test.ts`: 1 arquivo e 2 testes aprovados.
- `npm run check`: aprovado.
- `npm run build`: aprovado, com avisos de variáveis analíticas ausentes e chunk acima de 500 kB.
- Parse YAML via Ruby/Psych: aprovado.
- `git diff --check`: aprovado.
- Self-review: os três SHAs comprovados foram preservados; nenhuma action/versão funcional nova, alteração remota ou mudança nos artefatos preexistentes da Story 1.2 foi encontrada.
