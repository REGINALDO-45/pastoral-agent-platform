# Evidência externa — Governed Action Runtime — CI Run 32150384046

## Identificação

| Campo | Valor |
|---|---|
| Repositório | `REGINALDO-45/pastoral-agent-platform` |
| Branch | `agent/thanos-governed-action-runtime` |
| SHA validado | `d145d46e7c06b54be8d9ca096b699a8375b6a8cd` |
| Workflow | `THÁNOS Hermes Sandbox CI` |
| Job | `Isolated MySQL 8.4 sandbox` |
| Run | [32150384046](https://github.com/REGINALDO-45/pastoral-agent-platform/actions/runs/32150384046) |
| Disparo | `workflow_dispatch`, sem alteração de código do workflow |
| Resultado | `completed` / `success` |

## Gates observados

O job concluiu com sucesso todos os steps: boundary sandbox, serviço MySQL 8.4, migrations em banco temporário, regressão synthetic-safe, typecheck, build, verificação de ausência de configuração Hermes/piloto e limpeza dos containers efêmeros.

O log confirma **MySQL Community Server 8.4.11**. A regressão Vitest reportou **46 arquivos aprovados e 184 testes aprovados**, sem falhas. O step de typecheck executou `pnpm check` e o step de build executou `pnpm build`, ambos concluídos com sucesso.

A verificação final executou as condições `HERMES_ENABLED=false`, ausência de `HERMES_BASE_URL`, ausência de `HERMES_API_KEY` e `THANOS_PILOT_ENABLED=false`, emitindo `Hermes configuration remains absent and disabled.`. Não houve integração externa, efeito de produção, WhatsApp, Telegram, n8n ou Hermes real.

O workflow criou o MySQL como serviço efêmero local do job e removeu o container e a rede ao final. O warning de depreciação do Node.js 20 pertence às actions usadas pelo runner e não afetou a conclusão do job.

## Evidência local relacionada

A prova focused do runtime passou com 7 testes; a regressão synthetic-safe local passou com 45 arquivos e 175 testes. A diferença para o CI é explicada pelos 9 testes de integração pastorais dependentes de banco, que foram executados no MySQL efêmero do CI e elevaram o total para 46 arquivos e 184 testes.
